<h1>Indice de Posts</h1>

<table class="table">
        <tr>
            <th> Título </th>
            <th> Fecha </th>
            <th> Link a imagen </th>
            <th> Contenido </th>
        </tr>

    <% @posts.each do |post|%>
        <tr>
            <td><%= post.title %></td>
            <td><%= post.created_at.strftime("%d %b. %Y") %></td>
            <td><%= link_to post.image_url %></td>
            <td><%= post.content %></td>
        </tr>
    <%end%>

</table>
